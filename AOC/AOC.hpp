#pragma once
#include <string>
#include <vector>

namespace AOC {
    std::string fetchRawInput(int year, int day);
    std::vector<std::string> fetchAOCInputVector(int year, int day);
}  // namespace AOC